{
  "name": "nightwatch-minimal-config",
  "scripts": {
    "test": "npm run test:lint && npm run test:unit && npm run test:e2e",
    "test:lint": "eslint ./tests --max-warnings 0",
    "test:unit": "echo 'test:unit'",
    "test:e2e": "(cd tests && nightwatch --env firefox,chrome)"
  },
  "devDependencies": {
    "eslint": "^5.13.0",
    "nightwatch": "^1.0.18",
    "nightwatch-html-reporter": "^2.0.6"
  },
  "bin": {
    "nightwatch": "./node_modules/nightwatch/bin/nightwatch"
  }
}
